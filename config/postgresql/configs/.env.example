# -----------------------------------------------------------------------------
# PostgreSQL（共享資料庫）設定範本
# -----------------------------------------------------------------------------
# 注意：此檔案包含密碼，建議權限設為 600。
#
# 使用情境（Headscale / Tailscale 內網跨機共用）：
# - 在「資料庫主機」部署 postgresql app（預設只綁 127.0.0.1）
# - 透過「Headscale → Tailnet 服務埠轉發」把 TCP/<db_port> 轉發出去
# - 其他機器上的服務用 <db_host>:<db_port> 連入（db_host 建議填 tailnet IP）

POSTGRES_DB=${POSTGRES_DB}
POSTGRES_USER=${POSTGRES_USER}
POSTGRES_PASSWORD=${POSTGRES_PASSWORD}

# 建議使用 SCRAM（比 md5 更安全；並避免 trust）
# 注意：在部分 entrypoint 實作中，POSTGRES_INITDB_ARGS 可能被視為「單一參數」處理；
# 因此這裡建議只設定單一參數，避免空白造成 initdb 解析錯誤。
POSTGRES_INITDB_ARGS=${POSTGRES_INITDB_ARGS}
