# Netdata Cloud（可選）：要把 Agent 綁定到 Netdata Cloud 再填
# - 留空：不會自動 claim
# - 填寫：容器啟動後會嘗試 claim（需可連網）
NETDATA_CLAIM_TOKEN=
NETDATA_CLAIM_URL=https://app.netdata.cloud
NETDATA_CLAIM_ROOMS=

# 其他可選環境變數可自行加在此檔案（例如：proxy、TLS、額外設定）。
