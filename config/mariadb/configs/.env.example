# -----------------------------------------------------------------------------
# MariaDB（共享資料庫）設定範本
# -----------------------------------------------------------------------------
# 參考：
# - 官方 repo：https://github.com/MariaDB/mariadb-docker
# - 官方說明：https://mariadb.com/kb/en/mariadb-server-docker-official-image-environment-variables/
#
# 注意：
# - 此檔案包含密碼，建議權限設為 600。
# - 官方 image 說明：若資料目錄已初始化，以下初始化變數不會再次套用。
#
# 使用情境（Headscale / Tailscale 內網跨機共用）：
# - 在「資料庫主機」部署 mariadb app（預設只綁 127.0.0.1）
# - 透過「Headscale → Tailnet 服務埠轉發」把 TCP/<db_port> 轉發出去
# - 其他機器上的服務用 <db_host>:<db_port> 連入（db_host 建議填 tailnet IP）

MARIADB_ROOT_PASSWORD=${MARIADB_ROOT_PASSWORD}
MARIADB_DATABASE=${MARIADB_DATABASE}
MARIADB_USER=${MARIADB_USER}
MARIADB_PASSWORD=${MARIADB_PASSWORD}

# 若你需要 root 可從遠端登入（通常不建議），可自行取消註解：
#MARIADB_ROOT_HOST=%

# 若你不需要時區表，也可自行取消註解以縮短初始化時間：
#MARIADB_INITDB_SKIP_TZINFO=1
