# Kopia Repository 密碼（用於加密/解密；請妥善保存）
KOPIA_PASSWORD=${KOPIA_PASSWORD}

# Kopia Web UI（server start）基本認證
KOPIA_SERVER_USERNAME=${KOPIA_SERVER_USERNAME}
KOPIA_SERVER_PASSWORD=${KOPIA_SERVER_PASSWORD}

# 可選：時區（用於日誌顯示）
TZ=Asia/Taipei
