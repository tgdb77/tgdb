# -----------------------------------------------------------------------------
# Redis（共享快取/Queue）設定範本
# -----------------------------------------------------------------------------
# 注意：此檔案包含密碼，建議權限設為 600。
#
# 使用情境（Headscale / Tailscale 內網跨機共用）：
# - 在「Redis 主機」部署 redis app（預設只綁 127.0.0.1）
# - 透過「Headscale → Tailnet 服務埠轉發」把 TCP/<port> 轉發出去
# - 其他機器上的服務用 redis://:<pass>@<host>:<port> 連入（host 建議填 tailnet IP）

# Redis 密碼（必填）
REDIS_PASSWORD=${REDIS_PASSWORD}
