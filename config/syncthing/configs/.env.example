# -----------------------------------------------------------------------------
# Syncthing 環境變數範本
# -----------------------------------------------------------------------------
# 注意：此檔案可能包含敏感設定，建議權限設為 600。

# Web UI 綁定位置（通常不需要改）
STGUIADDRESS=0.0.0.0:8384

# 若映像支援 PUID/PGID，可用來對齊檔案權限（不支援則會被忽略）
PUID=${user_id}
PGID=${user_id}
