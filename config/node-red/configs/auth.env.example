# -----------------------------------------------------------------------------
# Node-RED 管理登入設定
# -----------------------------------------------------------------------------
# 注意：
# - 此檔案包含 Node-RED 管理介面帳密，建議權限維持 600。
# - TGDB 會在部署時讀取本檔並自動產生 /data/settings.js（bcrypt 雜湊）。
# - 修改後需重新部署此實例，新的帳密才會套用。

NODE_RED_ADMIN_USER=${user_name}
NODE_RED_ADMIN_PASSWORD=${pass_word}
