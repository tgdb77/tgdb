# -----------------------------------------------------------------------------
# Pinchflat 設定範本（環境變數）
# -----------------------------------------------------------------------------
# 注意：此檔案可能包含敏感資訊（例如 Basic Auth 密碼），建議權限設為 600。
#
# 來源：Pinchflat 官方 README 的「Environment variables」段落。
# 說明：
# - Pinchflat 的預設值已足夠使用；你只需要在有需求時再取消註解。
# - `ENABLE_IPV6` 與 `ENABLE_PROMETHEUS`：設定成任意「非空字串」即視為啟用。

# 時區（IANA 格式；預設 UTC）
TZ=Asia/Taipei

# 日誌層級（預設 debug；官方建議保留 debug）
#LOG_LEVEL=debug

# 檔案權限遮罩（預設 022；某些 NAS/Unraid 情境可能需要 000）
#UMASK=022

# -----------------------------------------------------------------------------
# 內建認證（Basic Auth）
# -----------------------------------------------------------------------------
# 注意：本專案部署流程預設「必須啟用」Basic Auth，請務必設定 username 與 password。
# 詳細行為請以官方文件的「Username and Password (authentication)」為準。
BASIC_AUTH_USERNAME=${user_name}
BASIC_AUTH_PASSWORD=${pass_word}

# -----------------------------------------------------------------------------
# RSS feed（預設 false）
# -----------------------------------------------------------------------------
# 讓 Pinchflat 對外提供 RSS feed 端點（可能影響曝露面，請自行評估）
#EXPOSE_FEED_ENDPOINTS=false

# -----------------------------------------------------------------------------
# IPv6（預設 false）
# -----------------------------------------------------------------------------
# 設成任意非空字串即可啟用（例如 true/1/yes）
#ENABLE_IPV6=

# -----------------------------------------------------------------------------
# SQLite 日誌模式（預設 wal）
# -----------------------------------------------------------------------------
# 若你把 /config 放在網路分享（NFS/SMB）上，SQLite 的 WAL 模式可能不穩定：
# - 可嘗試改成 delete（Rollback Journal）
# - 風險：從 wal 改成 delete 可能造成資料損毀風險，請先備份資料庫
JOURNAL_MODE=delete

# tzdata 資料庫路徑（預設 /etc/elixir_tzdata_data）
#TZ_DATA_DIR=/etc/elixir_tzdata_data

# -----------------------------------------------------------------------------
# 反向代理路徑前綴（預設 /）
# -----------------------------------------------------------------------------
# 若你把服務掛在子路徑（例如 https://example.com/pinchflat/），
# 可能需要設定 base route path，並在反代端「剝除」此前綴。
#BASE_ROUTE_PATH=/

# -----------------------------------------------------------------------------
# yt-dlp worker 併發（預設 2）
# -----------------------------------------------------------------------------
# 每個 queue 同時跑幾個使用 yt-dlp 的 worker。
# 若你遇到 IP limit，可設為 1；否則通常不要動。
#YT_DLP_WORKER_CONCURRENCY=2

# -----------------------------------------------------------------------------
# Prometheus（預設 false）
# -----------------------------------------------------------------------------
# 設成任意非空字串即可啟用（例如 true/1/yes）
#ENABLE_PROMETHEUS=
