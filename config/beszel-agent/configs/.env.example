# -----------------------------------------------------------------------------
# Beszel Agent 環境變數範本
# -----------------------------------------------------------------------------
# 注意：此檔案包含密碼/金鑰，建議權限設為 600。
#
# 提醒：
# - KEY/TOKEN 可能包含空白，因此此處使用雙引號包住，讓 systemd EnvironmentFile 可正確解析。

LISTEN=${host_port}

KEY=${KEY}
TOKEN=${TOKEN}
HUB_URL=${HUB_URL}
