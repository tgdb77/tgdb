# -----------------------------------------------------------------------------
# Bazarr（LinuxServer.io）環境變數範本
# -----------------------------------------------------------------------------
# 參考：
# - https://docs.linuxserver.io/images/docker-bazarr/

PUID=${user_id}
PGID=${group_id}
TZ=Asia/Taipei
UMASK=002

# 建議在 Bazarr 內把 Series / Movies 路徑都指向 /data/media/...。
