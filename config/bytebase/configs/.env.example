# -----------------------------------------------------------------------------
# Bytebase 環境變數範本
# -----------------------------------------------------------------------------
# 上游文件：
# - Docker 部署：
#   https://docs.bytebase.com/get-started/install/deploy-with-docker
# - 外部 PostgreSQL：
#   https://docs.bytebase.com/get-started/install/external-postgres
# - External URL：
#   https://docs.bytebase.com/get-started/self-host/external-url
#
# 說明：
# 1. Bytebase 預設會把 metadata 存在 ${instance_dir}/data（容器內 /var/opt/bytebase）。
# 2. 若你要改用外部 PostgreSQL 作為 Bytebase metadata DB，請設定 PG_URL。
# 3. 官方文件目前明確提供的環境變數重點是 PG_URL。
# 4. External URL 不是環境變數；請用 `--external-url` 旗標或進入 Web UI 設定。

# 外部 PostgreSQL 連線字串範例：
#PG_URL=postgresql://bytebase:your_password@db.example.com:5432/bytebase

# 若你使用檔案型 secret，也可把 PG_URL 設成「連線字串檔案路徑」。
# 例如：
#PG_URL=/run/secrets/bytebase-pg-url
