# -----------------------------------------------------------------------------
# AdGuard Home 環境變數範本
# -----------------------------------------------------------------------------
# 上游文件：
# - Docker：https://github.com/AdguardTeam/AdGuardHome/wiki/Docker
# - 環境變數：https://adguard-dns.io/kb/adguard-home/environment/
#
# 注意：
# 1. 依上游文件，目前公開列出的 AdGuard Home 支援環境變數只有
#    ADGUARD_HOME_DEFAULT_WEB_PORT。
# 2. 此變數只影響「首次安裝精靈」中建議帶入的 HTTP 埠。
# 3. TGDB 目前將宿主機 ${host_port} 映射到容器內 3000/tcp，因此這裡預設
#    使用 3000，避免初始安裝時改成其他容器內埠後，造成 PublishPort 對不上。
# 4. 若你改成其他值，請同步調整 Quadlet 內的 PublishPort 設定。

ADGUARD_HOME_DEFAULT_WEB_PORT=3000
